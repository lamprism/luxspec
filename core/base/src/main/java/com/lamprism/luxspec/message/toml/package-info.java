/**
 * Optional TOML file support for provider-independent message resolution.
 *
 * <p>Applications using this package must provide the Jackson TOML data format dependency at
 * runtime. The dependency is intentionally compile-only for the core module.</p>
 */
@NullMarked
package com.lamprism.luxspec.message.toml;

import org.jspecify.annotations.NullMarked;
